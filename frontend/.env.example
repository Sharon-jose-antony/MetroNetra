# MetroNetra Frontend — Environment Configuration Template
# Copy this file to .env or configure in your hosting environment

# API Backend Base URL (leave empty or use relative path if frontend is served by FastAPI on port 8000)
VITE_API_URL=http://localhost:8000
